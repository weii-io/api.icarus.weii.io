version: "3.9"

services:
  icarus:
    image: postgres:13
    restart: always
    ports:
      - "9999:5432"
    environment:
      POSTGRES_USER: uicarus
      POSTGRES_PASSWORD: meow
      POSTGRES_DB: icarus
    volumes:
      - ./data:/var/lib/postgresql/data